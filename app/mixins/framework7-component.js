import Ember from 'ember';

export default Ember.Mixin.create(
	{
		didRender () {
//			Ember.assert (
//				'Framework7 was not initialized, all framework7 application must be wrapped in f7-application ' +
//				'componnent and pass the desired parameters',
//				this.get ("framework7") !== undefined
//			);
//			this._super ();
		}
	}
);
